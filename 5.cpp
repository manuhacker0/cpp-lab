#include <iostream>
using namespace std;
int main()
{
    int a, b;
    cout << "Enter the value of a and b = ";
    cin >> a >> b;
    (a > b) ? (cout << "a is bigger \n") : (cout << "b is bigger \n");
}